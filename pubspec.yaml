name: liquid_glass
description: >
  Apple-inspired Liquid Glass UI effects for Flutter — refraction, dispersion,
  Fresnel reflection, blob merging, glare, and animated glass shapes.
version: 0.1.0
homepage: https://github.com/your-org/liquid_glass

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
